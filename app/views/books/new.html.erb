<% @user = current_user %>

<% if user_signed_in? && @user.admin %>

  <h1>New Book</h1>

  <%= render 'form', book: @book %>
<% else %>
  <br><br>
  You must be logged in as admin to add new books...
  <br><br>
<% end %>



<%= link_to 'Back', books_path %>
