<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

    <body>
        <h1>Hello, <%= @user.email %></h1>
        <p>
          You have the current books checked out, please make sure you return them on time, to avoid late fees.
        </p>
        <p>

          <table>
            <thead>
              <tr>
                <th>Title</th>
                <th>Checkout date</th>
                <th>Due date</th>
                <th>Status</th>

                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @user.checkedouts.each do |book| %>
                <tr>
                  <td> <%  title = Book.find(book.book_id) %><%= title.Title %></td>
                  <td><%= book.checkedout %></td>
                  <td><%= book.duedate %></td>
                  <td><%= book.checkedoutstatus %></td>

                </tr>
              <% end %>
            </tbody>
          </table>

        </p>
        <p>Thanks for using our local library system and have a great day!</p>
      </body>


</html>
